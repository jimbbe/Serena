# Apps

Application modules live here.

## Current modules

- **`core`**: Active product core with two implemented modules (`inbound-gate` for message classification and routing, `mediation-bridge` for session management between two participants) plus six T10 contract modules awaiting implementation (`contact-directory`, `session-manager`, `mediation-understanding`, `prudent-rewording`, `whatsapp-gateway`, `orchestrator`). 36 tests passing. See `apps/core/README.md`.
- **`gateway-wa`**: Future WhatsApp gateway or adapter (placeholder).
- **`panel`**: Future panel placeholder, only if a later task confirms it is needed.
