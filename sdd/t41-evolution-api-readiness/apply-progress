## Implementation Progress

**Change**: t41-evolution-api-readiness  
**Mode**: Strict TDD

### Completed Tasks
- [x] 1.1 Update `infra/vps/gateway-wa-staging/docker-compose.yml` for explicit private topology and no host-port/public-route intent.
- [x] 1.2 Refresh `infra/vps/gateway-wa-staging/.env.example` with placeholder-only runtime values including Redis placeholder.
- [x] 1.3 Keep `infra/vps/gateway-wa-staging/routing-table.example.json` minimal, non-secret, operator-only aligned.
- [x] 2.1 Update `docs/ops/t37-gateway-wa-staging-runbook.md` for operator-only management path and explicit exclusions.
- [x] 2.2 Update `docs/ops/t40-vps-core-readiness.md` with T41 handoff wording and boundary reminder.
- [x] 2.3 Update `README.md`, `docs/project-status.md`, `docs/open-questions.md` for resolved Evolution hosting decision + remaining risks.
- [x] 3.1 Tighten `scripts/smoke/gateway-wa-staging-smoke.ts` to non-destructive checks only.
- [x] 3.2 Add `scripts/tests/t41-evolution-api-readiness.test.ts` to enforce private/no-secret/non-destructive guardrails.
- [x] 3.3 Wire T41 validation into `package.json` / `npm run check`.
- [x] 4.1 Confirm exclusions are preserved (no deploy/pair/send/public admin/secrets).
- [x] 4.2 Keep OpenSpec task checklist aligned with final edits.

### Files Changed
| File | Action | What Was Done |
|---|---|---|
| `infra/vps/gateway-wa-staging/docker-compose.yml` | Modified | Added explicit private-topology comments and no-public intent without changing runtime behavior. |
| `infra/vps/gateway-wa-staging/.env.example` | Modified | Kept placeholder-only stance and added `REDIS_PASSWORD` placeholder. |
| `infra/vps/gateway-wa-staging/routing-table.example.json` | Modified | Added operator-only metadata and non-secret management note. |
| `docs/ops/t37-gateway-wa-staging-runbook.md` | Modified | Documented operator-only private path and explicit no-public-admin rule. |
| `docs/ops/t40-vps-core-readiness.md` | Modified | Clarified T41 as repo-only readiness and preserved core/gateway boundary. |
| `README.md` | Modified | Replaced stale next-step decision with resolved private Evolution hosting decision. |
| `docs/project-status.md` | Modified | Added resolved hosting decision and T41 readiness section. |
| `docs/open-questions.md` | Modified | Removed unresolved Evolution-hosting question and added T41 resolution entry. |
| `scripts/smoke/gateway-wa-staging-smoke.ts` | Modified | Updated T41 framing, required `GATEWAY_BASE_URL`, preserved non-destructive smoke checks. |
| `scripts/tests/t41-evolution-api-readiness.test.ts` | Created | Added guardrail tests for templates/docs/smoke constraints. |
| `package.json` | Modified | Added `validate:t41` and included it in `npm run check`. |
| `openspec/changes/t41-evolution-api-readiness/tasks.md` | Modified | Marked completed checklist items `[x]`. |
| `scripts/tests/t41-evolution-api-readiness.test.ts` | Modified | Added dedicated assertions for Core/Evolution boundary and restart limitation explicitness; kept guardrails repo-only. |

### TDD Cycle Evidence
| Task | Test File | Layer | Safety Net | RED | GREEN | TRIANGULATE | REFACTOR |
|---|---|---|---|---|---|---|---|
| 3.2 + 3.3 (initial batch) | `scripts/tests/t41-evolution-api-readiness.test.ts` | Unit/static | N/A (new file in initial batch) | ✅ Wrote failing guardrail assertions first (private topology/placeholders/docs/smoke text), then observed failure. | ✅ After docs/template updates, 4/4 T41 tests passed and `npm run check` passed. | ✅ Multiple scenarios in one file (topology + env + smoke + docs) prevented trivial pass. | ➖ None needed (repo-only guardrail assertions). |
| Follow-up warning 1: strict evidence auditability | `scripts/tests/t41-evolution-api-readiness.test.ts` | Unit/static | ✅ `node --experimental-strip-types --test scripts/tests/t41-evolution-api-readiness.test.ts` baseline passing before edits. | ✅ Added new dedicated assertions first (boundary + restart explicitness). | ✅ Focused test run passed 6/6 after implementation. | ✅ Added two new independent behaviors (Core/Evolution boundary scan + restart limitation wording). | ✅ Reworked evidence format to canonical columns (RED/GREEN/REFACTOR/TRIANGULATE/SAFETY NET). |
| Follow-up warning 2: partial scenarios from verify | `scripts/tests/t41-evolution-api-readiness.test.ts` | Unit/static | ✅ Same baseline above. | ✅ Wrote scenario-specific checks before code/docs adjustments. | ✅ `npm run check` passed including `validate:t41` now 6/6. | ✅ Distinct assertions prove both scenarios directly, no longer indirect-only. | ➖ None needed (assertion-level hardening only). |

### Follow-up Warning Closure (this apply batch)
- Added dedicated T41 assertion that scans `apps/core/src/**/*.ts` (excluding `*.test.ts`) and fails if Evolution endpoint/config patterns appear in Core source.
- Added dedicated T41 assertion that checks `docs/project-status.md` explicitly states in-memory instance tracking + restart-loss + future rehydration.
- Upgraded strict-TDD evidence table to canonical auditable format with explicit **Safety Net / RED / GREEN / TRIANGULATE / REFACTOR** columns.

### Deviations from Design
None — implementation matches design.

### Issues Found
- `openspec/changes/t41-evolution-api-readiness/tasks.md` appears as untracked in this workspace snapshot; the checklist was still updated in-place for T41 completion.

### Remaining Tasks
- [ ] None for the T41 apply checklist.

### Status
11/11 tasks complete. Ready for verify.
