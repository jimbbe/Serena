# Tests

Cross-module and acceptance tests can live here when they exist.

Module-local tests should stay near the module when that is clearer.

No product tests exist yet because no product behavior has been implemented.
